<UserControl x:Class="DanbooruTagsEditor.MVVM.View.ModifyTagsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DanbooruTagsEditor.MVVM.View"
             xmlns:View="clr-namespace:DanbooruTagsEditor.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="800"
             Background="#272537">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="55"/>
            <RowDefinition Height="55"/>
            <RowDefinition Height="280"/>
            <RowDefinition Height="80"/>
            <RowDefinition Height="250"/>
        </Grid.RowDefinitions>
        <!--Main Content-->
        <StackPanel Grid.Row="0" Grid.Column="1" Width="800" Height="55" Orientation="Horizontal" Margin="10" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Label Content="Paste the tags on the below text box." Width="800" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="24" Foreground="White" FontWeight="Bold" FontFamily="Verdana"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="1" Width="800" Orientation="Vertical">
            <TextBlock Margin="0 20 0 0" Text="Copy and paste the Danboorou tags here, then press Submit." Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="14" FontFamily="Verdana" FontWeight="Normal"/>
            <TextBlock Text="Prees X on the right to delete." Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="14" FontFamily="Verdana" FontWeight="Normal"/>
        </StackPanel>

        <!--Tag Text Box-->
        <StackPanel Grid.Row="2" Grid.Column="1" Width="760" Height="280" Margin="10 0 10 0">
            <View:ClearableScrollableTextBox x:Name="TagsTextBox" Foreground="NavajoWhite" FontSize="16" Height="230" Width="760" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <!--<TextBox x:Name="CSTextBox"  Style="{StaticResource InputTextBox}" Height="230" Width="760" HorizontalAlignment="Center" VerticalAlignment="Center" AcceptsReturn="True" AcceptsTab="True"></TextBox>-->
            <RadioButton x:Name="BtnSubmit" Style="{StaticResource ButtonRound}" FontSize="11" Content="Submit" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Foreground="#1e1d26" Width="80" Height="25" Margin="10" Click="BtnSubmit_Click"/>
        </StackPanel>

        <!--Modified Tags Section-->
        <StackPanel Grid.Row="3" Grid.Column="1" Margin="10">
            <Label Content="The Modified Tags:" HorizontalContentAlignment="Center" FontSize="18" Foreground="White" FontFamily="Verdana"></Label>
            <TextBlock Text="And Voila! The program will delete the '?' and numbers at the end for you, and return those new tags here." Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="14" FontFamily="Verdana" FontWeight="Normal"/>
        </StackPanel>
        <StackPanel Grid.Row="4" Grid.Column="1" Margin="10 0 10 0" Height="250" Width="760">
            <TextBox Text="{Binding ElementName=TagsTextBox, Path=ModifiedTags, Mode=OneWay}" IsReadOnly="True" Style="{StaticResource CustomTextBox}" 
                     Height="230" Width="760" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" 
                     AcceptsReturn="True"/>
        </StackPanel>
    </Grid>
</UserControl>
